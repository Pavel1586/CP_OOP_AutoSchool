<Page x:Class="AutoSchool.Views.Admin.AdminInstructorsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Инструкторы">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Инструкторы" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Instructors}"
                  SelectedItem="{Binding SelectedInstructor}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>

                <DataGridTemplateColumn Header="Фото" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="60" Height="60" CornerRadius="6" ClipToBounds="True">
                                <Image Stretch="UniformToFill"
                                       Source="{Binding Photo, Converter={StaticResource BytesToImage}}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="180"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="140"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Авто" Binding="{Binding Vehicle}" Width="180"/>
                <DataGridTextColumn Header="КПП" Binding="{Binding TransmissionText}" Width="90"/>
                <DataGridTextColumn Header="Кат." Binding="{Binding VehicleCategory}" Width="70"/>
                <DataGridTextColumn Header="Учеников" Binding="{Binding StudentsCount}" Width="90"/>

            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Добавить" Width="120" Height="35" Margin="5" Command="{Binding AddCommand}"/>
            <Button Content="Изменить" Width="120" Height="35" Margin="5" Command="{Binding EditCommand}"/>
            <Button Content="Удалить" Width="120" Height="35" Margin="5" Command="{Binding DeleteCommand}"/>
            <Button Content="Назад" Width="120" Height="35" Margin="5" Command="{Binding BackCommand}"/>
        </StackPanel>
    </Grid>
</Page>