<Window x:Class="AutoSchool.Views.Admin.QuestionEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вопрос"
        Height="280" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Текст вопроса" Margin="0,0,0,5"/>
        <TextBox x:Name="QuestionBox" Grid.Row="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

        <TextBlock Grid.Row="2" Text="Пояснение" Margin="0,10,0,5"/>
        <TextBox x:Name="ExplanationBox" Grid.Row="2" Margin="0,30,0,0"
                 Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Content="OK" Width="100" Height="35" Margin="5" Click="Ok_Click"/>
            <Button Content="Отмена" Width="100" Height="35" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>