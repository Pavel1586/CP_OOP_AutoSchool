<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!-- ================= COLORS ================= -->

	<Color x:Key="Win10AccentColor">#FF0078D7</Color>
	<Color x:Key="Win10AccentDarkColor">#FF005A9E</Color>

	<SolidColorBrush x:Key="Win10AccentBrush" Color="{StaticResource Win10AccentColor}" />
	<SolidColorBrush x:Key="Win10AccentDarkBrush" Color="{StaticResource Win10AccentDarkColor}" />

	<SolidColorBrush x:Key="Win10SurfaceBrush" Color="#EEFFFFFF" />
	<SolidColorBrush x:Key="Win10SurfaceSolidBrush" Color="#FFFFFFFF" />
	<SolidColorBrush x:Key="Win10TextBrush" Color="#FF1F1F1F" />
	<SolidColorBrush x:Key="Win10MutedTextBrush" Color="#FF6B6B6B" />
	<SolidColorBrush x:Key="Win10BorderBrush" Color="#FFD0D0D0" />

	<SolidColorBrush x:Key="Win10ControlBgBrush" Color="#FFF9F9F9" />
	<SolidColorBrush x:Key="Win10ControlBgHoverBrush" Color="#FFF2F2F2" />
	<SolidColorBrush x:Key="Win10ControlBgPressedBrush" Color="#FFE6E6E6" />

	<SolidColorBrush x:Key="Win10ErrorBrush" Color="#FFFF6347" />

	<CornerRadius x:Key="Win10CornerRadius">8</CornerRadius>

	<DropShadowEffect x:Key="AppCardShadow"
                      BlurRadius="18"
                      ShadowDepth="3"
                      Opacity="0.22"
                      Color="#000000" />

	<!-- ================= BASE ================= -->

	<Style TargetType="{x:Type FrameworkElement}">
		<Setter Property="TextElement.FontFamily" Value="Segoe UI" />
		<Setter Property="TextElement.FontSize" Value="14" />
		<Setter Property="TextElement.Foreground" Value="{StaticResource Win10TextBrush}" />
	</Style>

	<Style TargetType="{x:Type Window}">
		<Setter Property="Background" Value="{DynamicResource AppBackgroundBrush}" />
		<Setter Property="FontFamily" Value="Segoe UI" />
		<Setter Property="FontSize" Value="14" />
		<Setter Property="UseLayoutRounding" Value="True" />
		<Setter Property="SnapsToDevicePixels" Value="True" />
		<Setter Property="Cursor" Value="{DynamicResource AppCursor}" />
		<Setter Property="TextOptions.TextFormattingMode" Value="Display" />
		<Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
	</Style>

	<Style TargetType="{x:Type Page}">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="FontFamily" Value="Segoe UI" />
		<Setter Property="FontSize" Value="14" />
	</Style>

	<Style TargetType="{x:Type Frame}">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="NavigationUIVisibility" Value="Hidden" />
	</Style>

	<Style TargetType="{x:Type TextBlock}">
		<Setter Property="Foreground" Value="{StaticResource Win10TextBrush}" />
		<Setter Property="TextWrapping" Value="NoWrap" />
	</Style>

	<!-- ================= TEXT STYLES ================= -->

	<Style x:Key="AppTitleTextStyle" TargetType="{x:Type TextBlock}">
		<Setter Property="FontSize" Value="26" />
		<Setter Property="FontWeight" Value="Bold" />
		<Setter Property="HorizontalAlignment" Value="Center" />
		<Setter Property="Margin" Value="0,0,0,18" />
	</Style>

	<Style x:Key="AppSubtitleTextStyle" TargetType="{x:Type TextBlock}">
		<Setter Property="FontSize" Value="15" />
		<Setter Property="Foreground" Value="{StaticResource Win10MutedTextBrush}" />
		<Setter Property="TextWrapping" Value="Wrap" />
		<Setter Property="HorizontalAlignment" Value="Center" />
		<Setter Property="Margin" Value="0,0,0,18" />
	</Style>

	<Style x:Key="AppSectionTitleStyle" TargetType="{x:Type TextBlock}">
		<Setter Property="FontSize" Value="20" />
		<Setter Property="FontWeight" Value="Bold" />
		<Setter Property="Margin" Value="0,0,0,12" />
	</Style>

	<Style x:Key="AppFormLabelStyle" TargetType="{x:Type TextBlock}">
		<Setter Property="Foreground" Value="{StaticResource Win10MutedTextBrush}" />
		<Setter Property="Margin" Value="0,0,0,5" />
	</Style>

	<Style x:Key="AppErrorTextStyle" TargetType="{x:Type TextBlock}">
		<Setter Property="Foreground" Value="{StaticResource Win10ErrorBrush}" />
		<Setter Property="FontSize" Value="12" />
		<Setter Property="Margin" Value="0,4,0,0" />
		<Setter Property="Visibility" Value="Visible" />
		<Style.Triggers>
			<Trigger Property="Text" Value="">
				<Setter Property="Visibility" Value="Collapsed" />
			</Trigger>
			<Trigger Property="Text" Value="{x:Null}">
				<Setter Property="Visibility" Value="Collapsed" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<!-- ================= LAYOUT ================= -->

	<Style x:Key="AppRootGridStyle" TargetType="{x:Type Grid}">
		<Setter Property="Margin" Value="20" />
		<Setter Property="Background" Value="Transparent" />
	</Style>

	<Style x:Key="AppCardStyle" TargetType="{x:Type Border}">
		<Setter Property="Background" Value="{StaticResource Win10SurfaceBrush}" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="CornerRadius" Value="14" />
		<Setter Property="Padding" Value="22" />
		<Setter Property="Effect" Value="{StaticResource AppCardShadow}" />
	</Style>

	<Style x:Key="AppPanelStyle" TargetType="{x:Type Border}">
		<Setter Property="Background" Value="#F7FFFFFF" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="CornerRadius" Value="10" />
		<Setter Property="Padding" Value="14" />
		<Setter Property="Margin" Value="0,0,0,12" />
	</Style>

	<Style x:Key="AppBottomButtonsStyle" TargetType="{x:Type StackPanel}">
		<Setter Property="Orientation" Value="Horizontal" />
		<Setter Property="HorizontalAlignment" Value="Center" />
		<Setter Property="Margin" Value="0,18,0,0" />
	</Style>

	<!-- ================= BUTTONS ================= -->

	<ControlTemplate x:Key="Win10ButtonTemplate" TargetType="{x:Type Button}">
		<Border x:Name="Bd"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="7"
                SnapsToDevicePixels="True">
			<ContentPresenter Margin="{TemplateBinding Padding}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              RecognizesAccessKey="True">
				<ContentPresenter.Resources>
					<!-- Для строкового Content у Button генерится TextBlock: даем перенос, чтобы подписи не обрезались. -->
					<Style TargetType="{x:Type TextBlock}">
						<Setter Property="TextWrapping" Value="Wrap" />
						<Setter Property="TextTrimming" Value="CharacterEllipsis" />
					</Style>
				</ContentPresenter.Resources>
			</ContentPresenter>
		</Border>

		<ControlTemplate.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter TargetName="Bd" Property="Background" Value="{StaticResource Win10ControlBgHoverBrush}" />
			</Trigger>

			<Trigger Property="IsPressed" Value="True">
				<Setter TargetName="Bd" Property="Background" Value="{StaticResource Win10ControlBgPressedBrush}" />
			</Trigger>

			<Trigger Property="IsEnabled" Value="False">
				<Setter Property="Opacity" Value="0.55" />
			</Trigger>
		</ControlTemplate.Triggers>
	</ControlTemplate>

	<Style TargetType="{x:Type Button}">
		<Setter Property="Background" Value="{StaticResource Win10ControlBgBrush}" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Foreground" Value="{StaticResource Win10TextBrush}" />
		<Setter Property="Padding" Value="14,7" />
		<Setter Property="MinHeight" Value="34" />
		<Setter Property="MinWidth" Value="110" />
		<Setter Property="HorizontalContentAlignment" Value="Center" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="FocusVisualStyle" Value="{x:Null}" />
		<Setter Property="Cursor" Value="{DynamicResource AppCursor}" />
		<Setter Property="Template" Value="{StaticResource Win10ButtonTemplate}" />
	</Style>

	<Style x:Key="Win10PrimaryButton"
           TargetType="{x:Type Button}"
           BasedOn="{StaticResource {x:Type Button}}">
		<Setter Property="Background" Value="{StaticResource Win10AccentBrush}" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10AccentBrush}" />
		<Setter Property="Foreground" Value="White" />
	</Style>

	<!-- ================= INPUTS ================= -->

	<Style TargetType="{x:Type TextBox}">
		<Setter Property="Background" Value="White" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Padding" Value="8,5" />
		<Setter Property="MinHeight" Value="32" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="SelectionBrush" Value="{StaticResource Win10AccentBrush}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type TextBox}">
					<Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
						<ScrollViewer x:Name="PART_ContentHost" />
					</Border>

					<ControlTemplate.Triggers>
						<Trigger Property="IsKeyboardFocusWithin" Value="True">
							<Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Win10AccentBrush}" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.55" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type PasswordBox}">
		<Setter Property="Background" Value="White" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Padding" Value="8,5" />
		<Setter Property="MinHeight" Value="32" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type PasswordBox}">
					<Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
						<ScrollViewer x:Name="PART_ContentHost" />
					</Border>

					<ControlTemplate.Triggers>
						<Trigger Property="IsKeyboardFocusWithin" Value="True">
							<Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Win10AccentBrush}" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.55" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="AppValidationTextBoxStyle"
           TargetType="{x:Type TextBox}"
           BasedOn="{StaticResource {x:Type TextBox}}">
		<Setter Property="BorderBrush" Value="{StaticResource Win10ErrorBrush}" />
		<Style.Triggers>
			<Trigger Property="Tag" Value="">
				<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
			</Trigger>
			<Trigger Property="Tag" Value="{x:Null}">
				<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style x:Key="AppValidationPasswordBoxStyle"
           TargetType="{x:Type PasswordBox}"
           BasedOn="{StaticResource {x:Type PasswordBox}}">
		<Setter Property="BorderBrush" Value="{StaticResource Win10ErrorBrush}" />
		<Style.Triggers>
			<Trigger Property="Tag" Value="">
				<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
			</Trigger>
			<Trigger Property="Tag" Value="{x:Null}">
				<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style TargetType="{x:Type ComboBox}">
		<Setter Property="MinHeight" Value="32" />
		<Setter Property="Padding" Value="6,4" />
		<Setter Property="Background" Value="White" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
	</Style>

	<Style TargetType="{x:Type DatePicker}">
		<Setter Property="MinHeight" Value="32" />
	</Style>

	<!-- ================= LISTS / TABLES ================= -->

	<Style x:Key="WrapCellTextStyle" TargetType="{x:Type TextBlock}">
		<Setter Property="TextWrapping" Value="Wrap" />
		<Setter Property="TextTrimming" Value="None" />
		<Setter Property="VerticalAlignment" Value="Top" />
	</Style>

	<Style x:Key="WrapEditTextStyle" TargetType="{x:Type TextBox}">
		<Setter Property="TextWrapping" Value="Wrap" />
		<Setter Property="AcceptsReturn" Value="True" />
		<Setter Property="VerticalContentAlignment" Value="Top" />
		<Setter Property="Padding" Value="6" />
	</Style>

	<Style x:Key="WrapCellStyle" TargetType="{x:Type DataGridCell}">
		<Setter Property="Padding" Value="6,4" />
		<Setter Property="VerticalContentAlignment" Value="Top" />
	</Style>

	<Style TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource WrapCellStyle}" />

	<Style x:Key="WrapHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
		<Setter Property="HorizontalContentAlignment" Value="Center" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="FontWeight" Value="SemiBold" />
		<Setter Property="Background" Value="#FFF3F3F3" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
		<Setter Property="Padding" Value="8,6" />
		<Setter Property="ContentTemplate">
			<Setter.Value>
				<DataTemplate>
					<TextBlock Text="{Binding}"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               FontWeight="SemiBold" />
				</DataTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource WrapHeaderStyle}" />

	<Style TargetType="{x:Type DataGrid}">
		<Setter Property="Background" Value="White" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="RowBackground" Value="White" />
		<Setter Property="AlternatingRowBackground" Value="#FFF8F8F8" />
		<Setter Property="GridLinesVisibility" Value="Horizontal" />
		<Setter Property="HorizontalGridLinesBrush" Value="#FFE8E8E8" />
		<Setter Property="VerticalGridLinesBrush" Value="#FFE8E8E8" />
		<Setter Property="HeadersVisibility" Value="Column" />
		<Setter Property="CanUserAddRows" Value="False" />
		<Setter Property="AutoGenerateColumns" Value="False" />
		<Setter Property="IsReadOnly" Value="True" />
		<Setter Property="RowHeight" Value="NaN" />
		<Setter Property="MinRowHeight" Value="30" />
	</Style>

	<Style TargetType="{x:Type ListBox}">
		<Setter Property="Background" Value="White" />
		<Setter Property="BorderBrush" Value="{StaticResource Win10BorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Padding" Value="4" />
	</Style>

	<Style TargetType="{x:Type ListBoxItem}">
		<Setter Property="Padding" Value="8" />
		<Setter Property="Margin" Value="0,0,0,4" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
	</Style>

</ResourceDictionary>
